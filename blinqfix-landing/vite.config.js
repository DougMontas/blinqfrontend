// import { defineConfig } from 'vite';
// import react from '@vitejs/plugin-react';

// export default defineConfig({ plugins: [react()] });
// vite.config.js
export default {
    root: './src', // optional if using src as source
    build: {
      outDir: 'dist',
    },
  };
  